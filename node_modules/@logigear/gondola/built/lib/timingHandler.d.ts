export declare enum TimingOption {
    Visible = 0,
    Enable = 1,
    Clickable = 2
}
export declare function timing(options: TimingOption[]): (_: any, key: string, descriptor: PropertyDescriptor) => PropertyDescriptor;
